{
  "name": "local-service-lead-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lead generation and routing platform for local service businesses.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  }
}
